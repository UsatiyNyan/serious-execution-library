sl_gtest_prologue(v1.13.0)

sl_add_gtest(${PROJECT_NAME} model)
sl_add_gtest(${PROJECT_NAME} algo)
sl_add_gtest(${PROJECT_NAME} thread)
# sl_add_gtest(${PROJECT_NAME} coro)
# sl_add_gtest(${PROJECT_NAME} st)
sl_add_gtest(${PROJECT_NAME} exec)
